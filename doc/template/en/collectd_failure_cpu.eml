X-Mailer: %{app_name}
Subject: [Karesansui Notifier] failure threshold exceeded. CPU (ID:%{plugin_instance}) - %{type_instance}

Reported at: %{time}

The failure threshold for %{type_instance} of CPU (ID:%{plugin_instance}) on %{host} has been exceeded

The failure threshold is %{threshold_value}.
The current value is %{current_value}.


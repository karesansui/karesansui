X-Mailer: %{app_name}
Subject: [Karesansui Notifier] failure threshold exceeded. Network (interface:%{type_instance}) - %{type}

Reported at: %{time}

The failure threshold for %{type} of network (interface:%{type_instance}) on %{host} has been exceeded

The failure threshold is %{threshold_value}.
The current value is %{current_value}.


X-Mailer: %{app_name}
Subject: [Karesansui Notifier] warning threshold exceeded. Load average (%{ds})

Reported at: %{time}

The warning threshold for load average (%{ds}) on %{host} has been exceeded

The warning threshold is %{threshold_value}.
The current value is %{current_value}.


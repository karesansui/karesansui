X-Mailer: %{app_name}
Subject: [Karesansui Notifier] warning threshold exceeded. Network (interface:%{type_instance}) - %{type}

Reported at: %{time}

The warning threshold for %{type} of network (interface:%{type_instance}) on %{host} has been exceeded

The warning threshold is %{threshold_value}.
The current value is %{current_value}.


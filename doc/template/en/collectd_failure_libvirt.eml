X-Mailer: %{app_name}
Subject: [Karesansui Notifier] failure threshold exceeded. Virtual machine (%{host}) - %{type}

Reported at: %{time}

The failure threshold for %{type} of virtual machine (%{host}) has been exceeded
type instance: %{type_instance}

The failure threshold is %{threshold_value}.
The current value is %{current_value}.


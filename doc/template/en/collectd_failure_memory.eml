X-Mailer: %{app_name}
Subject: [Karesansui Notifier] failure threshold exceeded. Memory (%{type_instance})

Reported at: %{time}

The failure threshold for memory (%{type_instance}) on %{host} has been exceeded

The failure threshold is %{threshold_value}.
The current value is %{current_value}.


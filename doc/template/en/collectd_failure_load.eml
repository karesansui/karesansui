X-Mailer: %{app_name}
Subject: [Karesansui Notifier] failure threshold exceeded. Load average (%{ds})

Reported at: %{time}

The failure threshold for load average (%{ds}) on %{host} has been exceeded

The failure threshold is %{threshold_value}.
The current value is %{current_value}.


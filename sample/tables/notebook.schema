CREATE TABLE notebook (
        id INTEGER NOT NULL, 
        title VARCHAR(64) NOT NULL, 
        value TEXT NOT NULL, 
        created TIMESTAMP, 
        modified TIMESTAMP, 
        PRIMARY KEY (id)
);
